<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Platform version
    |--------------------------------------------------------------------------
    |
    | Ciian's own version, shown on the dashboard. This is the platform itself,
    | not any system built inside it, and it is a build-time fact rather than
    | configuration — bump it here when you cut a release.
    |
    */

    'version' => '0.1.0',

    /*
    |--------------------------------------------------------------------------
    | Release stage
    |--------------------------------------------------------------------------
    |
    | Shown beside the version. Set to null once the platform is out of beta and
    | the badge should stop appearing.
    |
    */

    'stage' => 'Beta',

];
